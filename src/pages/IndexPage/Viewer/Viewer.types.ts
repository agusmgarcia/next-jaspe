import { type ConstantsTypes } from "#src/utils";

type ViewerProps = {
  description: string;
  formatId: "story";
  stoneId: ConstantsTypes.StoneVariant;
  title: string;
};

export default ViewerProps;
